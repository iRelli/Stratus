import { APIMessage } from "discord-api-types";
import { Interaction, Message, MessageComponentCollectorOptions, ReactionCollectorOptions } from "discord.js";

export type MessageSender = ({interaction, messageOptions, paginator }) => Promise<Message | APIMessage>;
export type PaginatorCollectorFilter<T extends unknown[]> = ({ ...args: T }) => boolean | Promise<boolean>;

export interface PaginatorCollectorOptions<T extends unknown[]> extends CollectorOptions<T> {
  filter?: PaginatorCollectorFilter<T>;
};

export interface BasePaginatorOptions {
  messageSender: MessageSender;
  collectorOptions: PaginatorCollectorOptions;
};

export class BasePaginator {
  public constructor(interaction: Interaction, options: BasePaginatorCreationAttributes)
}